# This is the main Apache HTTP server configurtion file.
#
# {{ ansible_managed }}

ServerRoot "/etc/httpd"

Listen {{ http_port }}

Include conf.modules.d/*.conf

User apache
Group apache

ServerAdmin {{ admin }}@{{ ansible_hostname }}

<Directory />
    AllowOverride none
    Require all denied
</Directory>

DocumentRoot "{{ content_dir }}"

<Directory "{{ content_dir }}">
    AllowOverride None
    Require all granted
</Directory>

<Directory "{{ content_dir }}">
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>
