apiVersion: v1
kind: Pod
metadata:
  labels:
    app: webapp-color
  name: webapp-color
spec:
  containers:
  - image: registry.fritz.box/my/arm64v8/simple-webapp-color
    name: webapp-color
    ports:
      - containerPort: 8080
    envFrom:
      - configMapRef:
          name: webapp-config
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
